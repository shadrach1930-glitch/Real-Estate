# Tests

Cross-cutting and end-to-end tests for the PrimeHomes Lead Bot.

Unit tests for backend live under `backend/tests/`.
Frontend tests will live under `frontend/` (or here if preferred).

See `docs/Testing & QA Specification.md` for the full testing strategy.
